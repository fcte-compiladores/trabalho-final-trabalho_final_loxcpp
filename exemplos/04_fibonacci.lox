// Calcula e imprime os 10 primeiros números da sequência de Fibonacci.
// Como não temos funções ou arrays, faremos de forma iterativa.

var limit = 10;
var count = 0;
var a = 0;
var b = 1;

print "Calculando os 10 primeiros numeros de Fibonacci:";

while (count < limit) {
    print a;
    var temp = a;
    a = b;
    b = temp + b;
    count = count + 1;
}
